package sangria.schema

package object introspection {
//  val Type = ???
//  val Schema = ObjectType("__Schema",
//    description = """description: 'A GraphQL Schema defines the capabilities of a GraphQL
//                    | server. It exposes all available types and directives on
//                    | the server, as well as the entry points for query and
//                    | mutation operations.""".stripMargin,
//    fields = List(
//      Field("types", NonNullType(ListType(Type)), Some("A list of all types supported by this server."))
//    ))
}
